using Iviz.Msgs.VisualizationMsgs;

namespace Iviz.Controllers.Markers
{
    internal enum OrientationMode
    {
        Inherit = InteractiveMarkerControl.INHERIT,
        Fixed = InteractiveMarkerControl.FIXED,
        ViewFacing = InteractiveMarkerControl.VIEW_FACING
    }
}